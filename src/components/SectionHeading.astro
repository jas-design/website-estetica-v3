---
const { eyebrow, title, align = 'center', text } = Astro.props;
---

<div class:list={['section-heading', `section-heading--${align}`]} data-reveal>
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2>{title}</h2>
  {text && <p>{text}</p>}
</div>
